version: '3.8'
services:
  server:
    build:
      context: ./server
      target: development
    ports:
      - "8000:8000"
      - "8080:8080"
    restart: always
    tty: true
    environment:
      - SERVER_PORT=8080
    volumes:
      - ./server:/app